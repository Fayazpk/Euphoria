<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Collections | <%= @subcategory.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        .product-card {
            transition: all 0.3s ease;
            perspective: 1000px;
        }
        .product-card:hover {
            transform: translateY(-10px) rotateX(5deg);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        }
        .discount-badge {
            background: linear-gradient(to right, #ff6b6b, #ff9900);
        }
        .filter-sidebar {
            height: calc(100vh - 2rem);
            position: sticky;
            top: 1rem;
        }
    </style>
</head>
<body class="bg-gray-50">
    <div class="container mx-auto px-4 py-8">
        <!-- Breadcrumbs -->
        <nav class="flex mb-6" aria-label="Breadcrumb">
            <!-- Original breadcrumb code -->
        </nav>

        <div class="flex flex-col md:flex-row gap-8">
            <!-- Filter Sidebar -->
            <div class="md:w-64 flex-shrink-0">
                <div class="bg-white rounded-lg shadow-md p-6 filter-sidebar">
                    <h3 class="text-lg font-semibold mb-4">Sort By</h3>
                    <div class="space-y-3">
                        
                        <%= link_to usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'price_asc'), 
                            class: "block text-gray-600 hover:text-blue-600 #{params[:sort] == 'price_asc' ? 'text-blue-600 font-semibold' : ''}" do %>
                            <i class="fas fa-sort-amount-down-alt mr-2"></i>Price: Low to High
                        <% end %>
                        <%= link_to usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'price_desc'), 
                            class: "block text-gray-600 hover:text-blue-600 #{params[:sort] == 'price_desc' ? 'text-blue-600 font-semibold' : ''}" do %>
                            <i class="fas fa-sort-amount-up mr-2"></i>Price: High to Low
                        <% end %>
                        <%= link_to usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'newest'), 
                            class: "block text-gray-600 hover:text-blue-600 #{params[:sort] == 'newest' ? 'text-blue-600 font-semibold' : ''}" do %>
                            <i class="fas fa-calendar-alt mr-2"></i>Newest First
                        <% end %>
                        <%= link_to usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'discount'), 
                            class: "block text-gray-600 hover:text-blue-600 #{params[:sort] == 'discount' ? 'text-blue-600 font-semibold' : ''}" do %>
                            <i class="fas fa-percent mr-2"></i>Biggest Discount
                        <% end %>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="flex-1">
                <!-- Sort Bar Mobile -->
                <div class="md:hidden mb-6">
                    <select class="w-full p-2 border rounded-md" onchange="window.location.href=this.value">
                        
                        <option value="<%= usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'price_asc') %>"
                            <%= params[:sort] == 'price_asc' ? 'selected' : '' %>>Price: Low to High</option>
                        <option value="<%= usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'price_desc') %>"
                            <%= params[:sort] == 'price_desc' ? 'selected' : '' %>>Price: High to Low</option>
                        <option value="<%= usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'newest') %>"
                            <%= params[:sort] == 'newest' ? 'selected' : '' %>>Newest First</option>
                        <option value="<%= usermodule_category_subcategory_products_path(@subcategory.category_id, @subcategory, sort: 'discount') %>"
                            <%= params[:sort] == 'discount' ? 'selected' : '' %>>Biggest Discount</option>
                    </select>
                </div>

                <!-- Product Grid -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% @products.each do |product| %>
                        <%= link_to usermodule_category_subcategory_product_productviews_path(@subcategory.category, @subcategory, product), class: "contents" do %>
                            <div class="product-card bg-white rounded-lg shadow-md overflow-hidden transform transition-all duration-300 hover:scale-105 flex flex-col h-full">
                                <div class="relative">
                                    <% if product.images.any? %>
                                        <%= image_tag(product.images.first, class: "w-full h-72 object-cover") %>
                                    <% else %>
                                        <div class="w-full h-72 bg-gray-200 flex items-center justify-center">
                                            <i class="fas fa-image text-gray-400 text-6xl"></i>
                                        </div>
                                    <% end %>
                                    <div class="absolute top-4 right-4 discount-badge text-white px-3 py-1 rounded-full text-sm font-bold">
                                        <%= product.discount_percentage %>% OFF
                                    </div>
                                    <button class="absolute top-4 left-4 bg-white rounded-full w-10 h-10 flex items-center justify-center shadow-md">
                                        <i class="far fa-heart text-gray-600"></i>
                                    </button>
                                </div>
                                <div class="p-4 flex flex-col flex-grow">
                                    <h3 class="text-lg font-semibold text-gray-800 mb-2"><%= product.name %></h3>
                                    <p class="text-gray-600 text-sm mb-4 line-clamp-2 flex-grow"><%= product.description %></p>
                                    <div class="flex items-center justify-between mt-auto">
                                        <div>
                                            <div class="flex items-center">
                                                <span class="text-xl font-bold text-gray-900 mr-3">
                                                    $<%= product.final_price %>
                                                </span>
                                                <span class="line-through text-gray-400 text-sm">
                                                    $<%= product.base_price %>
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
