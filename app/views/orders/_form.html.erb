<!-- app/views/admin/orders/_form.html.erb -->
<%= form_with(model: [:admin, @order], local: true, class: "space-y-6") do |f| %>
  <% if @order.errors.any? %>
    <div class="bg-red-50 p-4 rounded mb-6">
      <h2 class="text-red-700 text-sm font-medium">
        <%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:
      </h2>
      <ul class="mt-2 list-disc list-inside text-sm text-red-600">
        <% @order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="grid grid-cols-2 gap-6">
    <div class="space-y-4">
      <h3 class="text-lg font-medium">Order Information</h3>
      
      <div>
        <%= f.label :status, class: "block text-sm font-medium text-gray-700" %>
        <%= f.select :status, Order.statuses.keys.map { |s| [s.titleize, s] }, 
            {}, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm" %>
      </div>

      <div>
        <%= f.label :payment_status, class: "block text-sm font-medium text-gray-700" %>
        <%= f.select :payment_status, Order.payment_statuses.keys.map { |s| [s.titleize, s] }, 
            {}, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm" %>
      </div>

      <div>
        <%= f.label :shipping_cost, class: "block text-sm font-medium text-gray-700" %>
        <%= f.number_field :shipping_cost, step: '0.01', class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm" %>
      </div>
    </div>

    <div class="space-y-4">
      <h3 class="text-lg font-medium">Customer Information</h3>
      
      <div>
        <%= f.label :full_name, class: "block text-sm font-medium text-gray-700" %>
        <%= f.text_field :full_name, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm" %>
      </div>

      <div>
        <%= f.label :phone_number, class: "block text-sm font-medium text-gray-700" %>
        <%= f.text_field :phone_number, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm" %>
      </div>

      <div>
        <%= f.label :address, class: "block text-sm font-medium text-gray-700" %>
        <%= f.text_area :address, rows: 3, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm" %>
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <%= f.label :city, class: "block text-sm font-medium text-gray-700" %>
          <%= f.text_field :city, class: